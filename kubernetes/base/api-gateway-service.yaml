# ==========================================
# Service Kubernetes pour API Gateway
# ==========================================
apiVersion: v1
kind: Service

metadata:
  name: api-gateway
  namespace: techshop

spec:
  # Service interne pour l’instant
  type: ClusterIP

  selector:
    app: api-gateway

  ports:
    - port: 3000
      targetPort: 3000